import 'package:flutter/material.dart';

class FeeHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('Fee hostory',
            style: TextStyle(fontSize: 32.0),
          )
      ),
    );
  }
}